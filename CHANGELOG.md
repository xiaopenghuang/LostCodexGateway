# 变更日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] — 2026-09-21

### 新增

- **多服务器与一键切换**。可以保存任意多台服务器（名称/主机/端口/用户/私钥/
  预期出口 IP 各自独立），并在「服务器」页随时切换当前使用的那一台。
  - 语义是**切换**而非同时连接：任一时刻只有一条隧道。切换为**硬切**——
    先干净断开旧隧道，再用新服务器重连，切换瞬间正在进行的请求会中断。
  - **切换失败不自动回滚**。回滚会让用户看到「点了切换但没变」，比明确报错更难排查。
    改为如实报错 + 记录 `previous_server_id`，界面上出现「切回上一个」按钮。
  - **未连接时切换只改选中项**，不触发连接——只是浏览/预选不该被动建隧道。
- **服务器延迟探测**（`test_servers`）。对全部服务器并发做 5 秒超时的 TCP 探测。
  界面上明确标注它测的是**到服务器 SSH 端口的往返时间，不是出口延迟**——
  它不建隧道、不认证、不发数据，只回答「哪台机器网络更近」。
- 全局设置命令 `save_settings`：本地端口与重连策略可在「设置」页修改。

### 变更（不兼容）

- **配置结构**：扁平的 `server` 对象改为 `servers: ServerProfile[]` + `active_server_id`。
  **旧配置会自动迁移**（见下），无需手动处理。
- **本地端口上移为全局设置**。`socks_port` 从 `server` 移到 `settings`，
  并新增 `settings.bridge_port`。这是刻意的类型约束：端口若留在 per-server 上，
  用户改一台就会破坏「切换后地址不变」这个前提，下游 Codex CLI 的
  `HTTP_PROXY` 会指向空气。**因为端口固定，切换服务器对 CLI 透明——不用改配置、不用重启。**
- **预期出口 IP 下移到每台服务器**（`verify.expected_egress_ip` →
  `ServerProfile.expected_egress_ip`）。不同出口的预期 IP 本就不同，
  放全局会导致「切到第二台就被第一台的预期 IP 判为异常」。
- `set_expected_egress_ip` 增加 `server_id` 参数（空串 = 当前选中项）。
- 命令 `save_server_config` 拆分为 `save_server` / `delete_server` /
  `switch_server` / `test_servers`。
- 快照新增 `previous_server_id`；状态机新增 `SWITCHING`。

### 移除

- **`proxy_mode` 字段**。它定义在 3 处（Rust 配置、TS 类型、开发夹具）却
  **没有任何读取方**，默认值 `"unset"` 与代码固定走 http_bridge 的实际行为不符，
  夹具里的第三个值 `"socks5_remote_dns"` 又是另一套说法。属于设计期遗留，
  迁移时直接丢弃。新增回归测试 `legacy_proxy_mode_is_dropped` 确保它不会复活。

### 修复

- **浅色主题下语义色文字对比度不达标**（新增的延迟列暴露出来）。
  根因在共享样式：`--ok` / `--warn` / `--err` / `--accent` / `--info`
  是按「当背景/边框/图形」调的，直接拿来写小号文字，浅色主题下只有 2.9–3.6:1。
  - 新增文字专用变体 `--ok-fg` / `--warn-fg` / `--err-fg` / `--accent-fg` / `--info-fg`：
    深色主题下与原值相同（原本就达标），浅色主题下加深。
  - 全局把 15 处 `color: var(--语义色)` 换成 `-fg` 变体；
    `background:` / `border-color:` 保持用原变量，视觉设计不变。
  - 取值按**最苛刻背景**（药丸自身半透明底色合成到高亮行上）卡线，
    避免「白卡片上没问题、一进高亮行就掉下去」这种只在特定位置暴露的缺陷。
  - 实测：修复前 7 项不达标 → 修复后仅剩 2 项（见「已知问题」）。
- 浅色主题 `--tx-3` 由 `#67718a` 调至 `#606a82`：原值在侧栏琥珀底
  与高亮行上只有 4.42 / 4.38:1。
- 侧栏状态卡配色改为直接映射状态类别：原先 `SWITCHING` 这类 warn 态
  文案写着「切换中」颜色却是灰的。
- **切换失败会把应用卡死在「切换中」，只能重启**（本版引入，发布前发现）
  - `switch_server` 先把状态推进到 `Switching`，但 `connect_impl` 的**预检**
    （Host Key 是否已确认 / 私钥文件是否存在 / SOCKS 端口是否被占 /
    ssh.exe 能否定位）全部在它把状态推进到目标状态**之前**就 `return Err`，
    这些失败不会自己落状态——对普通「连接」无害（状态没动），对切换是致命的。
  - 而 `Switching` 在界面上是个**死状态**：「连接」按钮被 `busy` 禁用、
    「断开」按钮被 `!canDisconnect` 禁用，`switch_server` 自身又拒绝在
    `Switching` 下再次切换。三者叠加，用户除了重启应用没有任何出路。
  - 最容易踩到的触发方式：已连接 A，切到一台**还没确认 Host Key** 的 B。
    这不是边角情况，是首次使用多服务器的必经路径。
  - 修法：新增 `state_after_failed_switch` / `leave_switching`，切换流程中
    任何失败都把状态落到 `Disconnected`（此时旧隧道已断开，描述准确）；
    若 `connect_impl` 已推进到 `Error` 则不覆盖（那个状态信息量更大）。
  - 顺带修正：`previous_server_id` 改到**落盘成功之后**才记录——保存失败时
    根本没切换，提前记录会让界面冒出一个点了就报错的「切回上一个」。

### 其他

- 「服务器」页重做为「列表 + 编辑 + 切换」：含当前项高亮、延迟列、
  全局端口只读展示、Host Key 卡片标明作用于当前选中服务器。
- 「设置」页新增本地端口与重连策略编辑；**隧道运行期间后端拒绝改端口**
  （正在跑的桥接层仍绑在旧端口上，配置先行改掉会让下游连到「配置说有、实际没有」的地址）。
- 单元测试 72 → 79，新增覆盖切换不变量（切服务器不改端口、改名不改 id、
  预期出口 IP 各自独立、删当前项后选中项不悬空）与「切换失败不得卡在 Switching」。
- 开发夹具新增 `switching` 场景（含超长名称/主机名/不可达项），用于视觉检查边界。
- 已产出 NSIS 安装包 `LostCodexGateway_0.3.0_x64-setup.exe`
  （3,081,696 B，SHA-256 `ef1ba41b…8a0e4`）。已核对打进去的是本版前端
  （内嵌资源名 `index-Bb2FDCga.js` / `index-Dccyrqej.css` 命中）且无开发夹具泄漏。
  **本版未重跑安装/卸载冒烟**（0.1.0 做过），见 `docs/acceptance-report.md` §5.7 / §5.8。

### 已知问题

- 深色主题主按钮（白字 `#4f8cff`，3.22:1）与危险按钮（白字 `#f4666a`，3.02:1）
  未达 WCAG AA 的 4.5:1。属 0.1.0 起的既有配色，改动会波及全局视觉，
  故本次未动。修法：按钮底色改用更深的 `--accent-lo`（→ 4.72:1）
  或 `--err` 的深色变体（`#c62f34` → 5.44:1）。

## [0.2.0] — 2026-09-21

### 变更（不兼容）

- **诊断数据结构**：客户端路由判定不再使用单一的「桥接是否有连接」布尔量，
  改为结构化证据 `BridgeEvidence { tunneled, attempts, has_rejects }`。
  依赖旧字段的外部消费方需同步调整。
- **桥接统计**：新增 `connections_total` / `connections_tunneled` 的语义区分。
  `connections_total` 现在是**连接尝试数**（含被拒与上游失败的），
  判定「流量是否真的进了隧道」必须用 `connections_tunneled`。
- **Mihomo 检测**：`MihomoDetection` 新增 `verge_path` 字段。

### 修复

- **CLI 路由判定会把「试图走网关但失败」报成「已验证」**（严重）
  - `build_clients` 的 `cli` 分支原先只判断「网关可能可达 && 桥接有过连接」，
    且用的是 `connections_total`——该计数在 TCP 刚连上来时就自增，
    此时目标是否合法、`ssh -D` 能否建连都还未知。
    于是「目标非法 / SOCKS5 建连失败 / 并发满」的尝试也被计入，
    把失败冒充成成功，且该分支永远得不出「异常」结论。
  - 桥接层新增 `connections_tunneled`，仅在**目标校验通过 + 下游 SOCKS5 建连成功
    + 已回 200** 之后自增，并记录脱敏的 `recent_targets`。
  - CLI 三态化：有 tunneled 且网关卡可达 → 已验证；仅有尝试或存在拒绝 → 异常；
    零动静 → 未验证。
  - 这与项目既定原则「无法确认即不得声称已验证」保持一致。
- **网关不可达时误报「路由配置异常」**
  - Desktop/IDE 的 `any_other` 分支原先无条件报异常。现已把「网关卡自身可用」
    作为报异常的必要条件——否则「本地网关没起来」会被误导成「路由配错了」。
- **Clash Verge 版本永远读不到（硬编码安装路径）**
  - 原先写死 `D:\Clash Verge\clash-verge.exe`，装在非默认盘符即失效。
  - 改为四层定位：注册表卸载项 `DisplayIcon` → 运行中进程映像路径 →
    常见安装目录（由环境变量与盘符动态推导）→ `PATH`；
    全部失败则如实报告「未定位到」，**不猜路径**。
  - 新增 `verge_path` 并在「应用」页展示，便于排查。

### 其他

- 应用内新增「Verge 安装路径」显示行。
- 文档补充路由判定矩阵（`docs/architecture.md` §3.4.1）。
- 新增 6 个单元测试，含两个针对上述误报的回归测试。

## [0.1.0] — 2026-09-21

### 新增

- 首个版本：SSH 动态转发（`-D`）出口隧道、OpenSSH 检测与指纹核对。
- HTTP CONNECT → SOCKS5 桥接层（Codex 原生二进制只接受 `HTTP(S)_PROXY` 的
  CONNECT 语义，不接受 `socks5://` 直供）。
- 网络诊断：本地 / 隧道 / 服务器 / 出口 / DNS / IPv6 / 客户端路由。
- Mihomo / Clash Verge 受控集成：只读检测、规则片段生成（不自动写入）、
  备份与回滚。
- UI：设计系统、深/浅双主题、左侧边栏导航。
